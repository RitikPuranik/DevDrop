const axios = require('axios');
function baseUrl(){const url=process.env.AI_SERVICE_URL;if(!url){const err=new Error('AI_SERVICE_URL is not configured');err.userMessage='AI Studio is not configured yet. Set AI_SERVICE_URL in backend/.env and restart.';err.statusCode=500;throw err;}return url.replace(/\/+$/,'');}
function headers(){return {'Content-Type':'application/json','X-Service-Key':process.env.AI_SERVICE_TOKEN||''};}
async function createJob({messages,fileData,websiteType,userData,preferences,assets,conversation}){try{const response=await axios.post(`${baseUrl()}/jobs`,{messages,fileData:fileData||null,websiteType:websiteType||'portfolio',userData:userData||{},preferences:preferences||{},assets:assets||[],conversation:conversation||messages},{headers:headers(),timeout:10000});return response.data?.data?.jobId;}catch(error){if(error.userMessage)throw error;const err=new Error(error.message);err.userMessage=error.response?.data?.message||'Failed to start AI generation.';err.statusCode=error.response?.status||502;throw err;}}
async function getJob(jobId){try{const response=await axios.get(`${baseUrl()}/jobs/${encodeURIComponent(jobId)}`,{headers:headers(),timeout:10000});return response.data?.data||null;}catch(error){if(error.response?.status===404)return null;if(error.userMessage)throw error;const err=new Error(error.message);err.userMessage=error.response?.data?.message||'Failed to check AI generation status.';err.statusCode=error.response?.status||502;throw err;}}
module.exports={createJob,getJob};
