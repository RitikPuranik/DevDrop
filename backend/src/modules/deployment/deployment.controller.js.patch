diff --git a/backend/src/modules/deployment/deployment.controller.js b/backend/src/modules/deployment/deployment.controller.js
--- a/backend/src/modules/deployment/deployment.controller.js
+++ b/backend/src/modules/deployment/deployment.controller.js
@@
-/** GET /api/deployments/providers/vercel/callback — public; Vercel echoes
- * our signed state token back on the `next` param (see vercel.provider.js). */
+/** GET /api/deployments/providers/vercel/callback — public; Vercel echoes
+ * our signed state token back on the `state` param (see vercel.provider.js). */
 const vercelCallback = async (req, res) => {
-  const { code, next: state, error: oauthError } = req.query;
+  const { code, state, error: oauthError } = req.query;
