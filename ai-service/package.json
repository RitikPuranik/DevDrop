{
  "name": "devdrop-ai-service",
  "version": "1.0.0",
  "description": "DevDrop AI Studio generation service (Gemini) — separated from the main backend so long-running generation never blocks it.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ext js,json,env server.js",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.11"
  }
}
